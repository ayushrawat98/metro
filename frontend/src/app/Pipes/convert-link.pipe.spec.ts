import { ConvertLinkPipe } from './convert-link.pipe';

describe('ConvertLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
