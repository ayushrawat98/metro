import { SortThreadPipe } from './sort-thread.pipe';

describe('SortThreadPipe', () => {
  it('create an instance', () => {
    const pipe = new SortThreadPipe();
    expect(pipe).toBeTruthy();
  });
});
